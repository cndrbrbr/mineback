__pycache__/
*.pyc
*.py[codz]

# vault runtime state — never checked in (real host is /var/lib/mineback by
# default; this only matters if someone points MINEBACK_HOME inside the repo)
/var-lib-mineback/
/vault-home/

.DS_Store
